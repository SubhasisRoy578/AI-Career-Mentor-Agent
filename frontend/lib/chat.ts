import { api } from './api';
export const chatApi = { list:(search?:string)=>api.get('/chat/conversations',{params:{search}}).then(r=>r.data.data), start:(data:{title?:string;message:string})=>api.post('/chat/conversations',data).then(r=>r.data.data), send:(id:string,message:string)=>api.post(`/chat/conversations/${id}/messages`,{message}).then(r=>r.data.data), get:(id:string)=>api.get(`/chat/conversations/${id}`).then(r=>r.data.data), remove:(id:string)=>api.delete(`/chat/conversations/${id}`).then(r=>r.data.data) };
export const interviewApi = { questions:(data:unknown)=>api.post('/interviews/questions',data).then(r=>r.data.data), save:(data:unknown)=>api.post('/interviews/sessions',data).then(r=>r.data.data), sessions:()=>api.get('/interviews/sessions').then(r=>r.data.data) };
