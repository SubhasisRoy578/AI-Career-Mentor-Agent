'use client';
import { useEffect, useState } from 'react';import { productivityApi } from '@/lib/productivity';
export default function Jobs(){const [jobs,setJobs]=useState<any[]>([]);const [form,setForm]=useState({company:'',role:'',status:'SAVED'});useEffect(()=>{productivityApi.jobs().then(setJobs).catch(()=>{})},[]);async function add(){await productivityApi.createJob(form);setJobs(await productivityApi.jobs());setForm({company:'',role:'',status:'SAVED'})}return <main className="space-y-6 p-6 text-white"><h1 className="text-3xl font-bold">Job Tracker</h1><section className="grid gap-3 rounded-3xl border border-white/10 bg-white/[.04] p-5 md:grid-cols-4"><input className="rounded-xl bg-slate-900 p-3" placeholder="Company" value={form.company} onChange={e=>setForm({...form,company:e.target.value})}/><input className="rounded-xl bg-slate-900 p-3" placeholder="Role" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}/><select className="rounded-xl bg-slate-900 p-3" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{['SAVED','APPLIED','INTERVIEW','OFFER','REJECTED','WITHDRAWN'].map(x=><option key={x}>{x}</option>)}</select><button className="rounded-xl bg-brand-500 p-3 font-semibold" onClick={add}>Add</button></section><div className="grid gap-3">{jobs.map(j=><article className="rounded-2xl border border-white/10 bg-slate-900 p-4" key={j.id}><p className="font-semibold">{j.role} · {j.company}</p><p className="text-slate-400">{j.status}</p></article>)}</div></main>}
