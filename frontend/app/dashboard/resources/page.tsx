'use client';
import { useEffect, useState } from 'react';import { productivityApi } from '@/lib/productivity';
export default function Resources(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{productivityApi.resources().then(setItems).catch(()=>{})},[]);return <main className="space-y-6 p-6 text-white"><h1 className="text-3xl font-bold">Career Resources</h1><p className="text-slate-400">Courses, certifications, books, documentation, YouTube, coding platforms, and open-source recommendations are served from curated production data.</p><div className="grid gap-4 md:grid-cols-2">{items.length?items.map(i=><a className="rounded-2xl border border-white/10 bg-slate-900 p-4" href={i.url ?? '#'} key={i.id}><p className="font-semibold">{i.title}</p><p className="text-slate-400">{i.type} · {i.provider}</p></a>):<p className="rounded-2xl border border-white/10 p-6 text-slate-400">No resources configured yet.</p>}</div></main>}
